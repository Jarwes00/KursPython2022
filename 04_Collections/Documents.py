list.copy()

list.sort(*, key=None, reverse=False)
# nie zmienia listy. Trzeba ją osobno podpisać V
sorted(iterable, /, *, key=None, reverse=False)

# czyści listę, nie usuwa
list.clear()

sum()

# 2 wymiarowa

for row in list 3x3:
print(row}